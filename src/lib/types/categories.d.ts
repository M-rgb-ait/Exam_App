declare type categoriestype = {
  // categories: {
  _id: string;
  image: string;
  productsCount: number;
  // };
};
declare type categoriesdetdels = {
  category: {
    _id: string;
    name: string;
    image: string;
  };
};
